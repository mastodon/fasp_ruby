<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Debug Provider" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="container mx-auto p-4 flex gap-8 bg-blue-100">
      <p class="font-bold">
        Debug Provider
      </p>
      <% if signed_in? %>
        <%= link_to t(".logs"), main_app.logs_path %>
        <%= link_to t(".subscriptions"), main_app.subscriptions_path %>
        <%= link_to t('.sign_out'), fasp_base.session_path, data: {turbo_method: :delete}, class: "hover:bg-blue-200" %>
      <% else %>
        <%= link_to t(".sign_up"), fasp_base.new_registration_path, class: "hover:bg-blue-200" %>
        <%= link_to t(".sign_in"), fasp_base.new_session_path, class: "hover:bg-blue-200" %>
      <% end %>
    </nav>
    <main class="container mx-auto mt-8 px-5">
      <%= tag.div notice if notice %>
      <%= tag.div alert if alert %>

      <%= yield %>
    </main>
  </body>
</html>
